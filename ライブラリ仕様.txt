ライブラリ仕様

★MKS単位系で統一
★回転行列はZXY順
★ワールド変換行列はスケール×回転×移動
★現段階では作成したバッファーは後からいじることはできない。

★中途半端にマルチプラットフォーム

★Soundを基底でMP3,OGG,WAVファイルを読み込むクラス←面倒だが分業が可能
  Soundのファイル読み込みに全部ぶっこむ←使うときとか楽だが、1人しかコードをいじれない。

★StateMachineの派生クラスポインタは全部unodererd_mapに格納
  ChangeStateしてなかった場合に生成するように。
    ★ここまでの部分はLoaderFactoryとかと同様。
    最後はツールやxmlから読み取ったデータで扱えるように見据えて置く。
    なので文字型で管理することにもなる。

★キャストする際はstatic_cast, dynamic_cast, const_cast, reinterpret_castを使うこと。

★多重継承した際、関数同士の名前が衝突する恐れがあるので純粋仮想関数で定義される関数の頭には
  大文字で「V」を付けること。 